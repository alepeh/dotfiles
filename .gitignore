backups
.zshrc.local

# Environment files with secrets
.env
.env.local
.env.mcp
.env.*
!.env.template

# MCP wrapper scripts with secrets
.mcp-wrappers/

# App-managed configs (machine-specific, track .example files instead)
claude/claude_desktop_config.json
amp/settings.json

# Hugo build artifacts
site/public/
site/resources/
site/.hugo_build.lock
